<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Model;

class Country extends Model
{
    /*
    I could have done the following, but for the purposes of the assignment it was not needed

    protected $table = 'countries'; // Optional, only needed if the table name is non-standard
    protected $fillable = ['name', 'code']; // Optional, used for mass assignment protection

    */
}
